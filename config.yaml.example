# 翻译器配置示例

# 默认翻译器: google, openai, local_llm
default_translator: openai

# 源语言和目标语言
source_lang: en
target_lang: zh

# Google Translate API 配置
google:
  # 设置环境变量 GOOGLE_APPLICATION_CREDENTIALS 指向服务账号密钥文件
  project_id: your-project-id

# OpenAI API 配置
openai:
  api_key: ${OPENAI_API_KEY}  # 从环境变量读取
  model: gpt-4o
  base_url: https://api.openai.com/v1
  # 学术翻译专用提示词（可选）
  # 如果不设置，将使用内置的优化提示词（基于"翻译即重写"理念，避免翻译腔和欧化表达）
  # 如需自定义，可在此处设置完整的提示词
  # system_prompt: |
  #   你的自定义提示词...

# 本地LLM配置 (vLLM, ollama等)
local_llm:
  base_url: http://localhost:8000/v1
  model: qwen2.5-72b-instruct
  api_key: not-needed  # 本地部署通常不需要
  # 学术翻译专用提示词（可选）
  # 如果不设置，将使用内置的优化提示词（基于"翻译即重写"理念，避免翻译腔和欧化表达）
  # 如需自定义，可在此处设置完整的提示词
  # system_prompt: |
  #   你的自定义提示词...

# PDF处理配置
pdf:
  # 是否保留原文（双语对照）
  bilingual: false
  # 字体缩放因子（翻译后文本可能更长/短）
  font_scale: 0.9
  # 备用字体（用于中文显示）
  fallback_font: null  # 留空则使用内置字体
